# Python dependencies for scripts/ directory
#
# Minimum Python version: 3.10 (for typing features)
#
# Core scripts use Python standard library only.
# R2/export scripts require additional packages:

# R2 dataset export dependencies
boto3>=1.35.0       # AWS S3-compatible client for Cloudflare R2
pyarrow>=18.0.0     # Parquet file creation with zstd compression

# To install all dependencies:
#   pip install -r requirements.txt
#
# To verify Python version:
#   python3 --version (should be 3.10+)
