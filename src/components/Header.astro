---
// Header component with navigation
const currentPath = Astro.url.pathname;
---

<header>
  <div class="container">
    <div class="header-content">
      <a href="/" class="logo">instrumetriq</a>
      <nav>
        <a href="/research" class:list={[currentPath === '/research' && 'active']}>Research</a>
        <a href="/dataset" class:list={[currentPath === '/dataset' && 'active']}>Dataset</a>
        <a href="/status" class:list={[currentPath === '/status' && 'active']}>Status</a>
        <a href="/updates" class:list={[currentPath === '/updates' && 'active']}>Updates</a>
        <a href="/contact" class:list={[currentPath === '/contact' && 'active']}>Contact</a>
      </nav>
    </div>
  </div>
</header>

<style>
  header {
    height: var(--header-height);
    border-bottom: 1px solid var(--color-border);
    position: sticky;
    top: 0;
    background-color: var(--color-bg);
    z-index: 100;
  }
  
  .header-content {
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: space-between;
  }
  
  .logo {
    font-size: 1.25rem;
    font-weight: 500;
    letter-spacing: -0.02em;
    color: var(--color-text);
  }
  
  .logo:hover {
    color: var(--color-text);
  }
  
  nav {
    display: flex;
    gap: var(--space-lg);
  }
  
  nav a {
    font-size: 0.95rem;
    color: var(--color-text-muted);
    position: relative;
  }
  
  nav a:hover,
  nav a.active {
    color: var(--color-text);
  }
  
  nav a.active::after {
    content: '';
    position: absolute;
    bottom: -0.5rem;
    left: 0;
    right: 0;
    height: 2px;
    background-color: var(--color-accent);
  }
  
  @media (max-width: 768px) {
    nav {
      gap: var(--space-md);
    }
    
    nav a {
      font-size: 0.875rem;
    }
  }
</style>
