---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout 
  title="Instrumetriq - Sentiment Data Collection"
  description="Instrumetriq collects and analyzes sentiment data from social media platforms"
>
  <div class="home-container">
    <!-- Hero Section -->
    <section class="hero">
      <span class="eyebrow">STUDY IN PROGRESS</span>
      <h1 class="title">instrumetriq</h1>
      <p class="subtitle">Measuring crypto narrative signals from X (Twitter)</p>
      <p class="description">
        We score public posts with a domain sentiment model, aggregate activity and silence into time windows, and compare the results with market factors. Findings are reported as measured—positive or negative.
      </p>
    </section>

    <!-- Lab Status Panel -->
    <section class="lab-status">
      <div class="status-grid">
        <div class="status-cell">
          <div class="status-label">STATUS</div>
          <div class="status-value">Active</div>
        </div>
        <div class="status-cell">
          <div class="status-label">PHASE</div>
          <div class="status-value">Infrastructure</div>
        </div>
        <div class="status-cell">
          <div class="status-label">RECORDS</div>
          <div class="status-value">Pending</div>
        </div>
      </div>
    </section>

    <!-- Navigation Cards -->
    <section class="cards">
      <a href="/research" class="card">
        <div class="card-content">
          <h3 class="card-title">Research</h3>
          <p class="card-desc">Methodology and collection framework</p>
          <p class="card-detail">Ethical data sourcing, analysis approach, and documentation standards</p>
        </div>
        <span class="card-arrow">→</span>
      </a>
      
      <a href="/dataset" class="card">
        <div class="card-content">
          <h3 class="card-title">Dataset</h3>
          <p class="card-desc">Structure and access information</p>
          <p class="card-detail">Schema, formats, licensing terms, and distribution details</p>
        </div>
        <span class="card-arrow">→</span>
      </a>
      
      <a href="/status" class="card">
        <div class="card-content">
          <h3 class="card-title">Status</h3>
          <p class="card-desc">Current collection metrics</p>
          <p class="card-detail">Collection statistics, operational state, and progress tracking</p>
        </div>
        <span class="card-arrow">→</span>
      </a>
    </section>
  </div>
</BaseLayout>

<style>
  /* Background Layers */
  .home-container::before {
    content: '';
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    height: 60vh;
    background: radial-gradient(ellipse 800px 400px at 50% 0%, rgba(0, 188, 212, 0.04), transparent 70%);
    pointer-events: none;
    z-index: -1;
  }
  
  .home-container {
    max-width: var(--max-width);
    margin: 0 auto;
    padding: 0 var(--space-4);
  }
  
  /* Hero Section */
  .hero {
    /* Reduce vertical padding to minimize 1080p scroll */
    padding: var(--gap-20) 0 var(--gap-24);
    text-align: center;
    max-width: 840px;
    margin: 0 auto;
  }
  
  .eyebrow {
    display: inline-block;
    font-size: var(--font-size-xs);
    font-weight: 600;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--text-dim);
    margin-bottom: var(--space-3);
    padding: var(--space-1) var(--space-2);
    border: 1px solid var(--border);
    border-radius: var(--radius-sm);
    background: var(--panel);
  }
  
  .title {
    font-size: var(--font-size-5xl);
    font-weight: 400;
    letter-spacing: -0.03em;
    color: var(--text);
    margin: 0 0 var(--space-4) 0;
    line-height: 1.1;
  }
  
  .subtitle {
    font-size: var(--font-size-xl);
    font-weight: 400;
    color: var(--text-muted);
    margin: 0 0 var(--space-4) 0;
    line-height: 1.4;
  }
  
  .description {
    font-size: var(--font-size-base);
    line-height: 1.6;
    color: var(--text-muted);
    margin: 0;
    max-width: 640px;
    margin-left: auto;
    margin-right: auto;
  }
  
  /* Lab Status Panel */
  .lab-status {
    /* Section gap contract: status→cards gap equals cards gutter */
    margin: var(--gap-24) 0 var(--gap-24);
    /* Slightly tighter panel padding to reduce page height */
    padding: var(--gap-20) var(--gap-24);
    background: linear-gradient(to bottom, var(--panel), var(--panel2));
    border: 1px solid var(--border2);
    border-left: 2px solid var(--border2);
    border-radius: var(--radius-md);
    box-shadow: var(--shadow-sm);
  }
  
  .status-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-5);
  }
  
  .status-cell {
    display: flex;
    flex-direction: column;
    gap: var(--space-1);
  }
  
  .status-label {
    font-size: var(--font-size-xs);
    font-weight: 600;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--text-dim);
  }
  
  .status-value {
    font-size: var(--font-size-base);
    font-weight: 500;
    color: var(--text);
  }
  
  /* Navigation Cards */
  .cards {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    /* Gutter must match status→cards gap */
    gap: var(--gap-24);
    /* Reduce bottom spacing so footer fits on 1080p */
    margin: 0 0 var(--gap-24) 0;
  }
  
  .card {
    position: relative;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    /* Slightly tighter card padding to reduce overall page height */
    padding: var(--gap-24);
    background: linear-gradient(to bottom, var(--panel), var(--panel2));
    border: 1px solid var(--border);
    border-radius: var(--radius-md);
    transition: all var(--transition-base);
    min-height: 180px;
    overflow: hidden;
  }
  
  .card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 2px;
    height: 100%;
    background: var(--border);
    transition: background var(--transition-base);
  }
  
  .card:hover {
    border-color: var(--border2);
    background: linear-gradient(to bottom, var(--panel2), var(--panel));
    transform: translateY(-2px);
    box-shadow: var(--shadow-md);
  }
  
  .card:hover::before {
    background: var(--accent);
  }
  
  .card:focus {
    outline: 2px solid var(--accent);
    outline-offset: 2px;
  }
  
  .card-content {
    flex: 1;
  }
  
  .card-title {
    font-size: var(--font-size-2xl);
    font-weight: 500;
    color: var(--text);
    margin: 0 0 var(--space-2) 0;
  }
  
  .card-desc {
    font-size: var(--font-size-sm);
    font-weight: 500;
    color: var(--text-muted);
    margin: 0 0 var(--space-2) 0;
  }
  
  .card-detail {
    font-size: var(--font-size-sm);
    line-height: 1.5;
    color: var(--text-dim);
    margin: 0;
  }
  
  .card-arrow {
    display: block;
    font-size: var(--font-size-2xl);
    color: var(--text-dim);
    transition: all var(--transition-base);
    margin-top: var(--space-3);
  }
  
  .card:hover .card-arrow {
    color: var(--accent);
    transform: translateX(4px);
  }
  
  /* Responsive Design */
  @media (max-width: 768px) {
    .home-container {
      padding: 0 var(--space-3);
    }
    
    .hero {
      padding: var(--gap-20) 0 var(--gap-24);
    }
    
    .title {
      font-size: var(--font-size-4xl);
    }
    
    .subtitle {
      font-size: var(--font-size-lg);
    }
    
    .lab-status {
      padding: var(--gap-20);
    }
    
    .status-grid {
      grid-template-columns: 1fr;
      gap: var(--space-3);
    }
    
    .cards {
      grid-template-columns: 1fr;
    }
  }
  
  @media (min-width: 1400px) {
    .cards {
      grid-template-columns: repeat(3, 1fr);
    }
  }
</style>
